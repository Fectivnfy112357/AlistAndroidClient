package com.textvision.alistclient.music.playback

/**
 * Placeholder — the real `MediaSessionService` is implemented in `MusicPlaybackService`
 * (Task 11). This empty class exists only so `PlaybackController` and `PlaybackIntents`
 * can be compiled in isolation.
 */
class MusicPlaybackService
